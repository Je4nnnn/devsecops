services:
  frontend:
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:${FRONTEND_HTTPS_PORT:-18443}:443"
    volumes:
      - ${TLS_CERT_DIR:-./nginx/ssl}:/etc/nginx/ssl:ro
      - ./frontend/nginx-https.conf:/etc/nginx/conf.d/default.conf:ro
